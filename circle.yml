##Customize the test machine
machine:
  ##Version of Node use
  node:
    version: 7.10.0
##Customize dependencies
dependencies:
  pre:
    - npm install angular-cli@latest -g
  override:
    - npm install

##Test override
test:
  override:
    - ng lint
    - ng test --watch=false
    - ng e2e --watch=false

##deployment
